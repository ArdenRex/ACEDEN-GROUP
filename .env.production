# AI Workflow Coordinator — marketing website environment (production)
# Values here are public by design (NEXT_PUBLIC_ prefix) — no secrets.

# Base URL of the product application. Every "Try it" / "Login" CTA on
# the marketing site routes here.
NEXT_PUBLIC_APP_URL=https://ai-workflow-coordinator-ardenrexs-projects.vercel.app

# Canonical URL of this marketing site itself, used for metadata,
# Open Graph tags, and the sitemap.
NEXT_PUBLIC_SITE_URL=https://acedengroup.com
